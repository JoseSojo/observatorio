
interface Props {
    user: any;
    userId: string;
}

export default function DataLaboral({ user, userId }: Props) {

    return (
        <div>

            DataLaboral {userId}

        </div>
    )
}
